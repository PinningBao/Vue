import { ISearchOperatorExecutor } from "./ISearchOperatorExecutor";
export declare class SearchOperator {
    private static Registry;
    aliases: Array<string>;
    exec: ISearchOperatorExecutor;
    parseValue: (value: any) => any;
    validateValue: (value: any) => void;
    constructor(aliases: string | Array<string>, exec: ISearchOperatorExecutor, parseValue?: (value: any) => any, validateValue?: (value: any) => void);
    private wrapExec(exec);
    getName(): string;
}
