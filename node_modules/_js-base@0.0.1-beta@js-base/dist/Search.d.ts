import { SearchCriteria } from "./SearchCriteria";
export declare class Search {
    criteria: SearchCriteria[];
    test(entity: Object): boolean;
    static parseString(querystring: string): Search;
    static parse(query: any): Search;
}
